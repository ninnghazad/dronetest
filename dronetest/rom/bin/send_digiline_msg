local channel = argv[1]
local msg = argv[2]
local pos = dronetest.active_systems[sys.id].pos

digiline:receptor_send(pos, digiline.rules.default, channel, msg)
print(sys:waitForDigilineMessage(channel,0,4))

return true