if drone == nil then
	print("this program can only run on drones.")
	return false
end

local function T() 
end
-- debug.sethook(function() print("AHA2") end,"",10) 
--xpcall(T)

--T=(function (...) return {select('#',...),...} end)(pcall(T))
--print(dump(T))
--debug.sethook(function() print("AHA") end,"",10) 
while true do 
	print("HANG"..sys.getTime()) 
end 

print("TEST")
return true

--[[
while true do
--	print("### 0 "..sys.id.." "..getId())
	sys.yield()
	if sys:receiveEvent({"input"}) ~= nil then
		print("RECEIVED INPUT, ABORT EPIC MOVES NOW!")
		break
	end
	for i=1,4,1 do
		os.sleep(0.5)
		drone.forward()
		os.sleep(0.5)
		drone.back()
		os.sleep(0.5)
		drone.turnLeft()
	end
	while true do
	end
end
return true
--]]